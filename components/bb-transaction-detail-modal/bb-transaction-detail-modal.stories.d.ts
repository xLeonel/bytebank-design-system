import { Meta, StoryObj } from '@storybook/web-components';
declare const meta: Meta;
export default meta;
type Story = StoryObj;
export declare const Open: Story;
export declare const PixTransaction: Story;
export declare const Closed: Story;
/** Com anexos — miniaturas clicáveis (abre ampliado) + botão para adicionar */
export declare const WithAttachments: Story;
/** Comprovante em PDF — miniatura com ícone; clique abre preview inline (iframe) */
export declare const WithPdfAttachment: Story;
